interface:
  display_name: "Wish Builder"
  short_description: "Validate and execute a Trellis-authored delivery plan"
  default_prompt: "Use $wish-builder to have gstack shape my product direction, Trellis prepare the task graph, and agents deliver the approved snapshot."
